import ReactUploadFile from 'react-upload-file'

const ProductFileUpload = (props) => {
  <ReactUploadFile 
    chooseFileButton={<Button> Hello </Button>} 
    uploadFileButton={<Button />} 
  />
}

export default ProductFileUpload